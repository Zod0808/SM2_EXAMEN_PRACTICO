# Variables de entorno para Railway
# Copia este archivo como .env y completa los valores

# Puerto (Railway lo asigna automáticamente)
PORT=3000
HOST=0.0.0.0

# Entorno de ejecución
NODE_ENV=production

# MongoDB Atlas Connection String
# Reemplaza con tu propia URL de MongoDB Atlas
MONGODB_URI=mongodb+srv://usuario:contraseña@cluster0.xxxxx.mongodb.net/ASISTENCIA?retryWrites=true&w=majority&appName=Cluster0

# Configuración de seguridad (opcional)
JWT_SECRET=tu_jwt_secret_aqui
BCRYPT_ROUNDS=12

# Railway Variables (se configuran automáticamente)
RAILWAY_ENVIRONMENT=production
RAILWAY_SERVICE_NAME=acees-group-backend
